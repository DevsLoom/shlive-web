export type DynamicPageType = {
    page: string;
    title: string;
    slug?: string;
    content?: string;
    status: string;
};

export type DynamicPageFormType = {
    page: string;
    title: string;
    content?: string;
    status: string;
};
